## Design

The local E2E command avoids external network calls so regression checks are stable. Real Streamable MCP remains validated by the Phase 6 smoke command.

The command returns a structured JSON report with each lifecycle step and invocation result.
