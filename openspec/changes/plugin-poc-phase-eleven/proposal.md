## Why

After phases 1-10, the POC needs a single local acceptance command to verify the core lifecycle quickly.

## What Changes

- Add `run-e2e` CLI command.
- Run publish, install, enable, credential, runtime start, tool invocation, data source invocation, skill context rendering and health check.
- Add acceptance usage docs and tests.
